extends ../layout 

block content
    h2= title

    include errors

    form(action="/books/" + book.id + "/edit", method="post")
        include form
        p
            label(for='title') Title
            input#title(name='title' type='text' value=`${book.title}`)
        p
            label(for='author') Author
            input#author(name='author' type='text' value=`${book.aurhor}`)
        p
            label(for='genre') Genre
            input#genre(name='genre' type='text' value=`${book.genre}`)
        p
            label(for='year') Year
            input#year(name='year' type='text' value=`${book.year}`)
        p
            input(type='submit' value='Update Book') Submit
    //- form(action="/books/" + book.id + "/delete", onsubmit='return confirm("Are you sure you want to delete this book?")' method="post")
        p
            //- a.button(href='/books') Cancel
        p
            //- input(type='submit' value='Delete Book')