extends ../layout 

block content
    p Are you sure you want to delete this book?
    h2=book.title
    form(method='post' action='/books/' + book.id + '/delete')
        a.button(href='update') ← No! Take me back
        input(type='submit' value='Delete' class="delete")  
        //- a.button(href='/') ← Yes, I'm sure &#43; 

